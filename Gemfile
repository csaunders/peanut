source 'https://rubygems.org'

ruby '2.1.5'

gem 'rack'
gem 'sinatra'
gem 'redis'
gem 'rack-cors'

gem 'capistrano'
gem 'capistrano-passenger'

group :development do
  gem 'dotenv'
end

group :production do
  gem 'dotenv-deployment'
end
